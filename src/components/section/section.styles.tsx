import { css } from '~/styles/css';

export const section = css({
	marginBottom: '48px',
	_last: {
		marginBottom: 0,
	},
});

export const headline = css({
	marginBottom: '0.5em',
});

export const content = css({
	marginLeft: '16px',
});
